import React, { useRef } from 'react';

const Home = () => {

    const ref = useRef()

    return ( 
        <div className='h-container'>
            <form>
                <span>Home</span>
            </form>
        </div>
     );
}
 
export default Home;